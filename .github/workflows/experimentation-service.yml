name: experimentation-service

on:
  pull_request:
    branches:
      - main
    paths:
      - "experimentation_service/**"
      - ".github/workflows/experimentation-service.yml"
      - ".pre-commit-config.yaml"
  workflow_dispatch:

concurrency:
  group: experimentation-service-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  ci-build:
    name: CI image build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set image name
        run: echo "CI_IMAGE=ghcr.io/${GITHUB_REPOSITORY,,}/experimentation-service-ci:${GITHUB_SHA}" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build CI image
        uses: docker/build-push-action@v6
        with:
          context: experimentation_service
          target: ci
          tags: ${{ env.CI_IMAGE }}
          push: true

  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: ci-build

    steps:
      - name: Set image name
        run: echo "CI_IMAGE=ghcr.io/${GITHUB_REPOSITORY,,}/experimentation-service-ci:${GITHUB_SHA}" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull CI image
        run: docker pull "$CI_IMAGE"

      - name: Ruff check
        run: docker run --rm "$CI_IMAGE" uv run ruff check .

      - name: Ruff format check
        run: docker run --rm "$CI_IMAGE" uv run ruff format --check .

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: ci-build

    steps:
      - name: Set image name
        run: echo "CI_IMAGE=ghcr.io/${GITHUB_REPOSITORY,,}/experimentation-service-ci:${GITHUB_SHA}" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull CI image
        run: docker pull "$CI_IMAGE"

      - name: Pytest
        run: docker run --rm "$CI_IMAGE" uv run pytest

  runtime-build:
    name: Runtime image build
    runs-on: ubuntu-latest
    needs:
      - lint
      - test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build runtime image
        uses: docker/build-push-action@v6
        with:
          context: experimentation_service
          target: runtime
          tags: experimentation-service:pr
          load: false
